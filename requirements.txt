# ═══════════════════════════════════════════════════════════════════
# 🤖 ربات پیام ناشناس — Anonymous Messenger Bot
# پیش‌نیازهای پروژه (Python 3.10+)
# ═══════════════════════════════════════════════════════════════════

# ─── کتابخانه اصلی ربات تلگرام ───
# نسخه 3.31.0 به بالا برای پشتیبانی از پارامتر style در دکمه‌ها الزامی است.
# (ButtonStyle.PRIMARY / SUCCESS / DANGER)
aiogram==3.31.0

# ─── دیتابیس SQLite ناهمگام (Async) ───
# برای اجرای کوئری‌ها بدون بلاک کردن event loop
aiosqlite==0.20.0

# ─── زمان‌بند (Scheduler) ───
# برای پاکسازی خودکار لینک‌های استفاده‌شده و به‌روزرسانی last_seen
APScheduler==3.10.4

# ─── وابستگی‌های داخلی (به‌صورت خودکار نصب می‌شوند، اما برای شفافیت ذکر شده) ───
aiohttp>=3.9.0,<4.0.0
magic-filter>=1.0.12
pydantic>=2.4.1,<3.0.0
typing-extensions>=4.7.0
tzlocal>=5.0.1
